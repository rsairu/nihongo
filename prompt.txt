Task:
Provide Japanese readings (よみがな) for words NOT in known_words.json.
CRITICAL: Exclude ALL words that exist in known_words.json (after normalization).
CRITICAL: Include ALL kanji words that do NOT exist in known_words.json.

Processing:
1. Full OCR on photo text (vertical writing support)
2. Break text into word units

Normalization (apply before checking known_words.json):
- Ignore katakana-only words
- Reduce to dictionary form (ignore conjugations, inflections; サ変 → noun form)
- Treat kanji/kana equivalents as same (if 出来る in known_words.json, ignore できる)
- Strip numeric prefixes (七年間 → 年間, 3回目 → 回目, 数ヶ月 → ヶ月)
- Strip non-lexical suffixes only (中/前/後/済み/以上/以下/未満) if core meaning preserved

Evaluation:
- Check normalized form against known_words.json
- CRITICAL: If normalized form exists in known_words.json → EXCLUDE (do NOT include)
- CRITICAL: If normalized form does NOT exist → MUST include (even if abstract, even if kanji-only)
- Do NOT ignore kanji words that are not in known_words.json

Output:
- For each valid word: word + hiragana reading
- Do NOT output words present in known_words.json, with or without notes

Flashcard Selection (top 2 if multiple unknown words):
- Priority: high-frequency everyday words > foundational core vocabulary > both combined

Flashcard Output (for top 2):
1. Word (original form)
2. Reading (hiragana)
3. Simple definition in Japanese (1-2 sentences)
4. Examples (2-3 sentences, varied contexts)
- Separate each word with blank line